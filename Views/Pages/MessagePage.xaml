<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Sphere.Views.Pages.MessagePage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Sphere.Converters"
    Title="MessagePage">

    <Grid BackgroundColor="White" RowDefinitions="Auto,*,Auto">
        <!--  🔹 Header: Avatar + Tên  -->
        <Grid
            Grid.Row="0"
            Padding="10,5,10,5"
            BackgroundColor="#f5f5f5"
            ColumnDefinitions="Auto,*, Auto"
            VerticalOptions="Center">

            <!--  Avatar  -->
            <Frame
                Padding="0"
                BackgroundColor="LightGray"
                CornerRadius="20"
                HasShadow="False"
                HeightRequest="40"
                WidthRequest="40">
                <Image Aspect="AspectFill" Source="{Binding PartnerAvatar}" />
            </Frame>

            <!--  Full name  -->
            <Label
                Grid.Column="1"
                Margin="10,0,0,0"
                FontAttributes="Bold"
                FontSize="16"
                Text="{Binding PartnerFullName}"
                TextColor="Black"
                VerticalOptions="Center" />
            <!--  Action buttons  -->
            <HorizontalStackLayout
                Grid.Column="2"
                HorizontalOptions="End"
                Spacing="15"
                VerticalOptions="Center">

                <!--  Gọi thoại  -->
                <ImageButton
                    BackgroundColor="Transparent"
                    HeightRequest="24"
                    WidthRequest="24">
                    <ImageButton.Source>
                        <FontImageSource
                            FontFamily="MaterialIcons"
                            Glyph="&#xF03F2;"
                            Size="24"
                            Color="#512BD4" />
                    </ImageButton.Source>
                </ImageButton>

                <!--  Gọi video  -->
                <ImageButton
                    BackgroundColor="Transparent"
                    HeightRequest="24"
                    WidthRequest="24">
                    <ImageButton.Source>
                        <FontImageSource
                            FontFamily="MaterialIcons"
                            Glyph="&#xF0567;"
                            Size="24"
                            Color="#512BD4" />
                    </ImageButton.Source>
                </ImageButton>

                <!--  Báo cáo  -->
                <ImageButton
                    BackgroundColor="Transparent"
                    HeightRequest="24"
                    WidthRequest="24">
                    <ImageButton.Source>
                        <FontImageSource
                            FontFamily="MaterialIcons"
                            Glyph="&#xF07C4;"
                            Size="24"
                            Color="#512BD4" />
                    </ImageButton.Source>
                </ImageButton>
            </HorizontalStackLayout>
        </Grid>
        <!--  Danh sách tin nhắn  -->
        <CollectionView
            x:Name="MessagesList"
            Grid.Row="1"
            Margin="10,10,10,0"
            IsGrouped="False"
            ItemsSource="{Binding Messages}"
            RemainingItemsThreshold="1"
            RemainingItemsThresholdReachedCommand="{Binding LoadOlderMessagesCommand}"
            SelectionMode="None"
            VerticalOptions="Fill">
            <!--  Đặt khoảng cách giữa item  -->
            <CollectionView.ItemsLayout>
                <LinearItemsLayout ItemSpacing="2" Orientation="Vertical" />
            </CollectionView.ItemsLayout>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid HorizontalOptions="{Binding IsMine, Converter={StaticResource BoolToLayoutOptionsConverter}}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="14" />
                        </Grid.ColumnDefinitions>

                        <!--  Bubble tin nhắn  -->
                        <Frame
                            Grid.Column="0"
                            Padding="10"
                            BackgroundColor="{Binding IsMine, Converter={StaticResource BoolToColorConverter}}"
                            BorderColor="Transparent"
                            HasShadow="False"
                            MaximumWidthRequest="250">
                            <Label Text="{Binding Content}" TextColor="{Binding IsMine, Converter={StaticResource BoolToTextColorConverter}}" />
                        </Frame>

                        <!--  Status icon  -->

                        <ActivityIndicator
                            Grid.Column="1"
                            Margin="2,0,0,0"
                            HeightRequest="14"
                            IsRunning="{Binding IsSending}"
                            IsVisible="{Binding IsSending}"
                            VerticalOptions="End"
                            WidthRequest="14"
                            Color="DodgerBlue" />
                        <Label
                            Grid.Column="1"
                            Margin="2,0,0,0"
                            FontAttributes="Bold"
                            FontFamily="MaterialIcons"
                            HorizontalTextAlignment="Center"
                            IsVisible="{Binding ShowStatusIcon}"
                            Text="{Binding StatusIcon}"
                            TextColor="DodgerBlue"
                            VerticalOptions="End" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>

        </CollectionView>


        <!--  Thanh nhập tin nhắn  -->
        <Grid
            x:Name="InputGrid"
            Grid.Row="2"
            Padding="10,5,10,10"
            ColumnDefinitions="Auto,Auto,*,Auto"
            ColumnSpacing="5"
            VerticalOptions="End">

            <!--  Nút vị trí  -->
            <ImageButton
                Command="{Binding SendLocationCommand}"
                HeightRequest="25"
                VerticalOptions="Center"
                WidthRequest="25">
                <ImageButton.Source>
                    <FontImageSource
                        FontFamily="MaterialIcons"
                        Glyph="&#xF034E;"
                        Size="25"
                        Color="#512BD4" />
                </ImageButton.Source>
            </ImageButton>

            <!--  Nút bộ sưu tập  -->
            <ImageButton
                Grid.Column="1"
                Command="{Binding OpenGalleryCommand}"
                HeightRequest="25"
                VerticalOptions="Center"
                WidthRequest="25">
                <ImageButton.Source>
                    <FontImageSource
                        FontFamily="MaterialIcons"
                        Glyph="&#xF02F9;"
                        Size="25"
                        Color="#512BD4" />
                </ImageButton.Source>
            </ImageButton>

            <!--  Ô nhập  -->
            <Frame Grid.Column="2" Padding="0">
                <Editor
                    x:Name="MessageEntry"
                    AutoSize="TextChanges"
                    BackgroundColor="Transparent"
                    MaxLength="1000"
                    MaximumHeightRequest="120"
                    Placeholder="Soạn tin nhắn..."
                    Text="{Binding CurrentMessage, Mode=TwoWay}" />
            </Frame>

            <!--  Nút stack (đính kèm)  -->
            <ImageButton
                Grid.Column="3"
                Command="{Binding AttachCommand}"
                HeightRequest="25"
                IsVisible="{Binding CurrentMessage, Converter={StaticResource StringIsNullOrEmptyToBoolConverter}}"
                VerticalOptions="Center"
                WidthRequest="25">
                <ImageButton.Source>
                    <FontImageSource
                        FontFamily="MaterialIcons"
                        Glyph="&#xF0C68;"
                        Size="25"
                        Color="#512BD4" />
                </ImageButton.Source>
            </ImageButton>

            <!--  Nút gửi  -->
            <ImageButton
                Grid.Column="3"
                Command="{Binding SendMessageCommand}"
                HeightRequest="25"
                IsVisible="{Binding CurrentMessage, Converter={StaticResource StringIsNotNullOrEmptyToBoolConverter}}"
                VerticalOptions="Center"
                WidthRequest="25">
                <ImageButton.Source>
                    <FontImageSource
                        FontFamily="MaterialIcons"
                        Glyph="&#xF1C4D;"
                        Size="25"
                        Color="#512BD4" />
                </ImageButton.Source>
            </ImageButton>
        </Grid>
    </Grid>
</ContentPage>